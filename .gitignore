# Environment variables
.env

# Output directory
/output/
/reports/*.json
/reports/*.pdf

# Python cache
__pycache__/
*.pyc

# Dataset files (large downloads — don't commit to the repo)
# Keep the CEAS dataset in data/ locally when running the analyzer.
/data/*.csv
/data/*.zip
